<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.easykoreanlearning">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".mainscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".select" />
        <activity android:name=".output" />
        <activity android:name=".mainMenu" />
        <activity android:name=".selectSimpleWord" />
        <activity android:name=".SelectSentencesActivity" />
        <activity android:name=".SelectGreetingActivity" />
        <activity android:name=".outputNoImage" />
        <activity android:name=".outputWithImage"></activity>
    </application>

</manifest>